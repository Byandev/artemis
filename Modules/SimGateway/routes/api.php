<?php

/*
|--------------------------------------------------------------------------
| SimGateway API routes
|--------------------------------------------------------------------------
|
| Intentionally empty. SimGateway is consumed in-process by Artemis (SMS is a
| service call / Eloquent query), not over an external API. Programmatic access,
| if ever needed, should reuse Artemis's existing workspace API keys.
*/
