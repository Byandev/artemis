<?php

// Billing is administered from the platform admin area — see the `admin.`
// route group in routes/workspaces.php. Per-workspace access is gated by the
// `billing_module_enabled` toggle on the workspace.
